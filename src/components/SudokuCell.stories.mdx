import { action } from '@storybook/addon-actions';
import { withKnobs, number } from '@storybook/addon-knobs/react';
import { withA11y } from '@storybook/addon-a11y';
import SudokuCell from './SudokuCell';
import { Meta, Story } from '@storybook/addon-docs/blocks';

<Meta component={SudokuCell} title='Cell' decorators={[withKnobs, withA11y]} />

export const cellData = {
  cellID: 1,
  value: 1,
  row: 1,
  col: 1,
  className: 'cell',
  selected: false,
};
export const actionsData = {
  onClick: action('onClick'),
};
<Story name='Empty'>{() => {
    return <SudokuCell {...cellData} value={null} {...actionsData} />;
  }}</Story>

<Story name='ElementFilled'><SudokuCell
    {...cellData}
    value={number('Value', 1)}
    row={number('Row', 1)}
    col={number('Column', 1)}
    {...actionsData}></SudokuCell></Story>

<Story name='OuterCorner'>{() => {
    return (
      <div>
        <SudokuCell {...cellData} row={0} col={0} {...actionsData} />
      </div>
    );
  }}</Story>

<Story name='InnerCorner'>{() => {
    return (
      <div>
        <SudokuCell {...cellData} row={2} col={2} {...actionsData} />
      </div>
    );
  }}</Story>

<Story name='Selected'>{() => {
    return (
      <div>
        <SudokuCell {...cellData} selected={true} {...actionsData} />
      </div>
    );
  }}</Story>
