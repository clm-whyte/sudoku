import { Story, Preview } from "@storybook/addon-docs/blocks";
import SudokuGrid from "./SudokuGrid.js";

# Grid

The Grid component currently handles the most of the logic for rendering the Cells.  
The grid is a 2D array of cells which contain a `value` and `selected` property.

Since the cell is a functional component, all of the props that the cell requires are passed to it via the Grid component.

<Preview>
  <Story id="grid--grid" />
</Preview>
